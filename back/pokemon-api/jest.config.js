module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    // Path to your test files
    testMatch: ["**/tests/**/*.ts"],
    // If you're using a setup file to configure the environment, uncomment the next line
    // setupFilesAfterEnv: ['./jest.setup.js'], 
  };
  